# Highlight module-level mutable bindings

- Add a `meta.module-level-mutable.definition.eidos` grammar scope for `mut name (: Type)? := expr;`, highlighting the binding name and `:=` operator, and assert it in the editor contract test.
- Part of the C->Eidos module-level mutable global work item, targeting `0.9.0-alpha.2`.
