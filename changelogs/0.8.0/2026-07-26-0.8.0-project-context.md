# Eidos Language 0.8.0 project context

- sync-id: `eidosc-2026-07-26-stdlib-lsp-format-project-context`
- Added a single locked active-project context shared by LSP initialization, build, run, and package commands.
- Activating an Eidos source under another `eidos.toml` switches the project, while browsing Std, generated, documentation, or other non-project files preserves the current command target.
- Added standard workspace-folder synchronization, the Eidos project-context notification, and a status-bar indicator showing the locked project name and path.
