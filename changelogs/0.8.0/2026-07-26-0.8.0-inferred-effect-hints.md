# Inferred effect hints

- Render LSP virtual `need ...` clauses for compiler-inferred function effects.
- Add `eidosc.inlayHints.inferredEffects` so effect hints can be configured independently from local variable type hints.
- Surface the compiler's materialize-effect code action through the existing LSP code-action provider.
