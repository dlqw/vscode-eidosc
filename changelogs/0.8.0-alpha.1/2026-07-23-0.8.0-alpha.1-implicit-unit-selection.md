# 0.8.0-alpha.1 implicit Unit / selection support

- sync-id: `eidos-2026-07-23-implicit-unit-selection`
- Highlight `then` / `else` selection payload placeholders such as `_0` as pattern variables.
- Track the Eidos 0.8 implicit `Unit` body and selection grammar contract without changing `if` support.
