# Eidos Language Marketplace branding

- Publish the extension under the visible name **Eidos Language** and the extension id `eidos-language`.
- Use the Eidos owl mark for the Marketplace listing and the `.eidos` / `eidos.toml` language icons.
- Add Marketplace gallery metadata while preserving the independent `0.8.0-alpha.1` plugin version.
- Extend the protected release workflow to verify `VSCE_PAT`, publish the VS Code Marketplace prerelease, and attach the same `eidos-language` VSIX to the GitHub release.
