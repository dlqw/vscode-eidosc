# Typed declaration tags

- Highlight `@[repr(...), derive(...), expand(...), extern(...)]` groups as the Eidos 0.7 declaration attachment surface.
- Stop advertising removed `@borrow`, `requires`, `before`, and `after` metadata.
- Describe ownership information as a signature-derived compiler contract.
